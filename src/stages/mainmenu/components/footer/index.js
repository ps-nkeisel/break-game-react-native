import React from 'react';
import { BackgroundFooter } from '../../../../styles/style';

import UserControl from '../../../../components/userControl/index';

const bottomBackground = (props) => {
  return (
    <BackgroundFooter>
      <UserControl />
    </BackgroundFooter>
  )
};


export default bottomBackground;
